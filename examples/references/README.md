# Reference images

`fire_aoe_synthetic.png` is a procedurally generated stand-in for a
"giant circular fire AOE" reference (orange ring, central column, embers on
dark ground). It exists so the Mode A pipeline (analysis -> graph ->
render -> compare_reference) can be tested without third-party imagery.
Real reference images belong to their owners; keep only images we have the
right to redistribute here.
